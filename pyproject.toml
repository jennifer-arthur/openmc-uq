[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "openmc-uq"
version = "0.1.0"
description = "Uncertainty quantification tools for OpenMC"
requires-python = ">=3.11"
dependencies = []
